# 互动通知功能部署指南

## 快速部署步骤

### 第一步：执行数据库脚本

在 MySQL 中执行以下 SQL：

```bash
# 连接到数据库
mysql -u root -p

# 选择数据库
use your_database_name;

# 执行 SQL 文件
source notification-table.sql;

# 或者直接执行 SQL
```

```sql
CREATE TABLE `notification` (
  `notification_id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '通知ID',
  `user_id` BIGINT(20) NOT NULL COMMENT '接收通知的用户ID',
  `from_user_id` BIGINT(20) NOT NULL COMMENT '触发通知的用户ID',
  `type` VARCHAR(20) NOT NULL COMMENT '通知类型：like-点赞，comment-评论',
  `record_id` BIGINT(20) NOT NULL COMMENT '关联的打卡记录ID',
  `content` VARCHAR(500) DEFAULT NULL COMMENT '通知内容（评论内容）',
  `is_read` TINYINT(1) DEFAULT 0 COMMENT '是否已读：0-未读，1-已读',
  `created_at` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `read_at` DATETIME DEFAULT NULL COMMENT '阅读时间',
  PRIMARY KEY (`notification_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_is_read` (`is_read`),
  KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='通知表';
```

### 第二步：验证后端文件

确保以下文件已创建：

**实体类和 Mapper**:
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/domain/Notification.java`
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/mapper/NotificationMapper.java`
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/resources/mapper/web/NotificationMapper.xml`

**Service 层**:
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/service/INotificationService.java`
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/service/impl/NotificationServiceImpl.java`

**Controller**:
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/controller/NotificationController.java`

**修改的文件**:
- ✅ `RuoYi-Vue/ruoyi-admin/src/main/java/com/ruoyi/web/service/impl/InteractionServiceImpl.java`

### 第三步：重新编译后端

```bash
cd RuoYi-Vue
mvn clean package
```

### 第四步：重启后端服务

```bash
cd ruoyi-admin
mvn spring-boot:run
```

或者如果使用 JAR 包部署：

```bash
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

### 第五步：验证前端文件

确保以下文件已修改：

- ✅ `couple-fitness-weapp/utils/api.js` - 添加了 notificationAPI
- ✅ `couple-fitness-weapp/app.js` - 添加了 checkNotifications 方法

### 第六步：测试功能

#### 测试1：点赞通知

1. 使用用户A登录小程序
2. 点赞用户B的打卡记录
3. 切换到用户B的账号
4. 从后台切换到前台（或重新打开小程序）
5. **预期结果**：看到 Toast 提示"收到1条新通知"，手机震动

#### 测试2：评论通知

1. 使用用户A登录小程序
2. 评论用户B的打卡记录
3. 切换到用户B的账号
4. 从后台切换到前台
5. **预期结果**：看到 Toast 提示"收到1条新通知"，手机震动

#### 测试3：API 测试

使用 Postman 或其他工具测试 API：

**获取未读通知数量**:
```
GET http://localhost:8080/api/notification/unread/count
Authorization: Bearer {token}
```

**获取未读通知列表**:
```
GET http://localhost:8080/api/notification/unread
Authorization: Bearer {token}
```

**标记为已读**:
```
POST http://localhost:8080/api/notification/read/{notificationId}
Authorization: Bearer {token}
```

## 常见问题排查

### 问题1：编译错误

**错误信息**: `找不到符号 NotificationMapper`

**解决方案**:
1. 确保所有文件都已创建
2. 刷新 Maven 项目
3. 重新编译

### 问题2：数据库连接错误

**错误信息**: `Table 'notification' doesn't exist`

**解决方案**:
1. 确认已执行 SQL 脚本
2. 检查数据库名称是否正确
3. 验证表是否创建成功：
```sql
SHOW TABLES LIKE 'notification';
DESC notification;
```

### 问题3：通知不显示

**可能原因**:
1. 用户未登录
2. Token 过期
3. 网络请求失败

**排查步骤**:
1. 打开微信开发者工具的控制台
2. 查看是否有错误日志
3. 检查网络请求是否成功
4. 验证 Token 是否有效

### 问题4：自己点赞自己收到通知

**原因**: 通知创建逻辑有问题

**检查**:
```java
// 在 InteractionServiceImpl 中确认有这段代码
if (record.getUserId().equals(fromUserId)) {
    return; // 不给自己创建通知
}
```

## 验证清单

部署完成后，请逐项验证：

### 后端验证

- [ ] 数据库表 `notification` 已创建
- [ ] 后端服务启动成功，无错误日志
- [ ] API `/api/notification/unread/count` 可访问
- [ ] API `/api/notification/unread` 可访问
- [ ] 点赞后数据库中有通知记录
- [ ] 评论后数据库中有通知记录

### 前端验证

- [ ] 小程序启动无错误
- [ ] 切换到前台时调用通知检查
- [ ] 有新通知时显示 Toast
- [ ] 有新通知时手机震动
- [ ] 未登录时不检查通知

### 功能验证

- [ ] 用户A点赞用户B，用户B收到通知
- [ ] 用户A评论用户B，用户B收到通知
- [ ] 用户自己点赞自己，不创建通知
- [ ] 多次切换应用，通知检查正常
- [ ] 网络异常时不影响应用使用

## 性能监控

### 数据库查询性能

检查通知查询的性能：

```sql
-- 查看索引是否生效
EXPLAIN SELECT * FROM notification WHERE user_id = 1 AND is_read = 0;

-- 查看查询执行时间
SELECT SQL_NO_CACHE * FROM notification WHERE user_id = 1 ORDER BY created_at DESC LIMIT 20;
```

### API 响应时间

在微信开发者工具的 Network 面板中查看：
- `/api/notification/unread/count` 应该 < 200ms
- `/api/notification/unread` 应该 < 500ms

## 日志查看

### 后端日志

查看后端日志，确认通知创建成功：

```bash
tail -f logs/ruoyi-admin.log | grep -i notification
```

应该看到类似的日志：
```
[INFO] 创建点赞通知: recordId=123, fromUserId=1, toUserId=2
[INFO] 创建评论通知: recordId=123, fromUserId=1, toUserId=2, content=很棒！
```

### 前端日志

在微信开发者工具的控制台中查看：
```javascript
console.log('检查通知失败:', err);
```

## 下一步

功能部署成功后，可以考虑：

1. **创建通知列表页面**
   - 显示所有通知
   - 支持点击跳转
   - 支持标记已读

2. **优化通知提示**
   - 在首页显示通知横幅
   - 在 TabBar 显示未读徽标
   - 支持通知声音

3. **添加通知设置**
   - 开启/关闭通知
   - 设置免打扰时间
   - 选择通知类型

## 技术支持

如果遇到问题，请检查：

1. **文档**
   - `互动通知功能实现完成.md` - 完整实现说明
   - `互动通知功能实现方案.md` - 设计方案

2. **日志**
   - 后端日志：`logs/ruoyi-admin.log`
   - 前端控制台：微信开发者工具

3. **数据库**
   - 检查通知表数据
   - 验证索引是否生效

## 总结

按照以上步骤完成部署后，互动通知功能应该可以正常工作。用户在收到点赞或评论时，会在切换到小程序时看到通知提示。
